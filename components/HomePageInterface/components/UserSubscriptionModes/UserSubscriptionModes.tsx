const UserSubscriptionModes = () => {
  return (
    <div className="border-2 flex flex-col border-black md:mx-2 h-100">
      <div>
        <span className="text-3xl">User Subscription Modes</span>
      </div>
      <div>
        This is early user Subscription Modes: Sub Cards and explanation
        interface for both iterations of Home Page will be removed or swapped
        with another interface if user is max sub,if not we can use this space
        to promote subscription and show benefits of subscribing/upgrading to
        the user
      </div>
    </div>
  );
};
export default UserSubscriptionModes;
