const TenantSubscriptionModes = () => {
  return (
    <div
      id="subscription-modes-tenant"
      className="border-2 flex flex-col border-black md:mx-2 h-100"
    >
      <div>
        <span className="text-3xl">Tenant Subscription Modes</span>
      </div>
      <div>
        This is early tenant Subscription Modes: Sub Cards and explanation
        interface for both iterations of Home Page ill be removed or swapped
        with another interface if tenant user is max sub,if not we can use this
        space to promote subscription and show benefits of subscribing/upgrading
        to the tenant user
      </div>
    </div>
  );
};
export default TenantSubscriptionModes;
